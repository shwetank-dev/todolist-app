{
  "name": "@todolist/worker",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "tsx --env-file=.env src/worker.ts",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "db:migrate": "graphile-worker --schema-only",
    "test": "vitest run"
  },
  "dependencies": {
    "@todolist/shared": "workspace:*",
    "graphile-worker": "^0.16.6"
  },
  "devDependencies": {
    "@types/node": "^25.6.0",
    "@types/pg": "^8.20.0",
    "dotenv": "^17.4.2",
    "pg": "^8.20.0",
    "vitest": "^4.1.4"
  }
}
